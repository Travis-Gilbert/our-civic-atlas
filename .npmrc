; Railway's production builders can omit optional dependencies unless the
; project opts in explicitly. Next/Tailwind's CSS pipeline needs the
; platform-specific Lightning CSS binary available during `npm ci && npm run build`.
include=optional
