{
  "name": "porchfest-board-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Board-facing PorchFest dashboard. A self-contained Observable Framework static site bridged into the Next app at /porchfest/dashboard. Data loaders refresh the dashboard output without shipping database or service credentials to the browser.",
  "scripts": {
    "clean": "observable clean",
    "dev": "observable preview",
    "build": "observable build"
  },
  "dependencies": {
    "@observablehq/framework": "^1.13.0",
    "csv-parse": "^7.0.0",
    "googleapis": "^173.0.0",
    "postgres": "^3.4.5",
    "yjs": "^13.6.31"
  },
  "engines": {
    "node": ">=18"
  }
}
